diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/Model.js node_modules_patch/@sencha/ext-core/src/data/Model.js
--- ../node_modules/@sencha/ext-core/src/data/Model.js	2026-04-04 02:40:27.354893938 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/Model.js	2023-09-11 15:59:42.000000000 +0000
@@ -111,7 +111,7 @@
  * values.
  *
  * # Associations
- * 
+ *
  * Models often have associations with other Models. These associations can be defined by
  * fields (often called "foreign keys") or by other data such as a many-to-many (or "matrix").
  * See {@link Ext.data.schema.Association} for information about configuring and using associations.
@@ -173,7 +173,7 @@
  *             console.log('The User was destroyed!');
  *         }
  *     });
- * 
+ *
  * # HTTP Parameter names when using a {@link Ext.data.proxy.Ajax Ajax proxy}
  *
  * By default, the model ID is specified in an HTTP parameter named `id`. To change the
@@ -298,7 +298,7 @@
         me.internalId = internalId = modelIdentifier.generate();
 
         //<debug>
-        var dataId = data[idProperty]; // eslint-disable-line vars-on-top
+        var dataId = data[idProperty]; // eslint-disable-line vars-on-top, one-var
 
         if (session && !session.isSession) {
             Ext.raise('Bad Model constructor argument 2 - "session" is not a Session');
@@ -383,7 +383,7 @@
      * shortened name.
      *
      * All entities in a given `schema` must have a unique `entityName`.
-     * 
+     *
      * For more details see "Relative Naming" in {@link Ext.data.schema.Schema}.
      */
 
@@ -611,13 +611,13 @@
     // @cmd-auto-dependency {aliasPrefix: "data.field."}
     /**
      * @cfg {Object[]/String[]} fields
-     * An Array of `Ext.data.field.Field` config objects, simply the field 
-     * {@link Ext.data.field.Field#name name}, or a mix of config objects and strings. 
+     * An Array of `Ext.data.field.Field` config objects, simply the field
+     * {@link Ext.data.field.Field#name name}, or a mix of config objects and strings.
      * If just a name is given, the field type defaults to `auto`.
-     * 
-     * In a {@link Ext.data.field.Field Field} config object you may pass the alias of 
+     *
+     * In a {@link Ext.data.field.Field Field} config object you may pass the alias of
      * the `Ext.data.field.*` type using the `type` config option.
-     * 
+     *
      *     // two fields are set:
      *     // - an 'auto' field with a name of 'firstName'
      *     // - and an Ext.data.field.Integer field with a name of 'age'
@@ -625,13 +625,13 @@
      *         type: 'int',
      *         name: 'age'
      *     }]
-     * 
-     * Fields will automatically be created at read time for any for any keys in the 
-     * data passed to the Model's {@link #proxy proxy's} 
-     * {@link Ext.data.reader.Reader reader} whose name is not explicitly configured in 
+     *
+     * Fields will automatically be created at read time for any for any keys in the
+     * data passed to the Model's {@link #proxy proxy's}
+     * {@link Ext.data.reader.Reader reader} whose name is not explicitly configured in
      * the `fields` config.
-     * 
-     * Extending a Model class will inherit all the `fields` from the superclass / 
+     *
+     * Extending a Model class will inherit all the `fields` from the superclass /
      * ancestor classes.
      */
     /**
@@ -647,9 +647,9 @@
      * This property is indexed by field name and contains the ordinal of that field. The
      * ordinal often has meaning to servers and is derived based on the position in the
      * `fields` array.
-     * 
+     *
      * This can be used like so:
-     * 
+     *
      *      Ext.define('MyApp.models.User', {
      *          extend: 'Ext.data.Model',
      *
@@ -657,9 +657,9 @@
      *              { name: 'name' }
      *          ]
      *      });
-     * 
+     *
      *      var nameOrdinal = MyApp.models.User.fieldOrdinals.name;
-     *      
+     *
      *      // or, if you have an instance:
      *
      *      var user = new MyApp.models.User();
@@ -763,13 +763,13 @@
      * If specified, this is the name of the property that contains the entity "version".
      * The version property is used to manage a long-running transaction and allows the
      * detection of simultaneous modification.
-     * 
+     *
      * The way a version property is used is that the client receives the version as it
      * would any other entity property. When saving an entity, this property is always
      * included in the request and the server uses the value in a "conditional update".
      * If the current version of the entity on the server matches the version property
      * sent by the client, the update is allowed. Otherwise, the update fails.
-     * 
+     *
      * On successful update, both the client and server increment the version. This is
      * done on the server in the conditional update and on the client when it receives a
      * success on its update request.
@@ -798,17 +798,17 @@
 
     /**
      * @cfg {Boolean} convertOnSet
-     * Set to `false` to prevent any converters from being called on fields specified in 
+     * Set to `false` to prevent any converters from being called on fields specified in
      * a {@link Ext.data.Model#set set} operation.
-     * 
-     * **Note:** Setting the config to `false` will only prevent the convert / calculate 
-     * call when the set `fieldName` param matches the field's `{@link #name}`.  In the 
-     * following example the calls to set `salary` will not execute the convert method 
-     * on `set` while the calls to set `vested` will execute the convert method on the 
+     *
+     * **Note:** Setting the config to `false` will only prevent the convert / calculate
+     * call when the set `fieldName` param matches the field's `{@link #name}`.  In the
+     * following example the calls to set `salary` will not execute the convert method
+     * on `set` while the calls to set `vested` will execute the convert method on the
      * initial read as well as on `set`.
-     * 
+     *
      * Example model definition:
-     * 
+     *
      *     Ext.define('MyApp.model.Employee', {
      *         extend: 'Ext.data.Model',
      *         fields: ['yearsOfService', {
@@ -826,15 +826,15 @@
      *         }],
      *         convertOnSet: false
      *     });
-     *     
+     *
      *     var tina = Ext.create('MyApp.model.Employee', {
      *         salary: 50000,
      *         yearsOfService: 3
      *     });
-     *     
+     *
      *     console.log(tina.get('salary')); // logs 55000
      *     console.log(tina.get('vested')); // logs false
-     *     
+     *
      *     tina.set({
      *         salary: 60000,
      *         yearsOfService: 4
@@ -912,7 +912,7 @@
             name = field.name;
             prop = field.summaryField || name;
 
-            if (name !== this.idProperty) {
+            if (name !== 'id') {
                 /* eslint-disable-next-line max-len */
                 result[name] = summary ? summary.calculate(records, prop, 'data', 0, recLen) : undefined;
             }
@@ -1087,35 +1087,35 @@
 
     /**
      * Sets the given field to the given value. For example:
-     * 
+     *
      *      record.set('name', 'value');
-     * 
+     *
      * This method can also be passed an object containing multiple values to set at once.
      * For example:
-     * 
+     *
      *      record.set({
      *          name: 'value',
      *          age: 42
      *      });
-     * 
+     *
      * The following store events are fired when the modified record belongs to a store:
      *
      *  - {@link Ext.data.Store#event-beginupdate beginupdate}
      *  - {@link Ext.data.Store#event-update update}
      *  - {@link Ext.data.Store#event-endupdate endupdate}
-     * 
-     * @param {String/Object} fieldName The field to set, or an object containing key/value 
+     *
+     * @param {String/Object} fieldName The field to set, or an object containing key/value
      * pairs.
      * @param {Object} newValue The value for the field (if `fieldName` is a string).
      * @param {Object} [options] Options for governing this update.
-     * @param {Boolean} [options.convert=true] Set to `false` to  prevent any converters from 
-     * being called during the set operation. This may be useful when setting a large bunch of 
+     * @param {Boolean} [options.convert=true] Set to `false` to  prevent any converters from
+     * being called during the set operation. This may be useful when setting a large bunch of
      * raw values.
      * @param {Boolean} [options.dirty=true] Pass `false` if the field values are to be
      * understood as non-dirty (fresh from the server). When `true`, this change will be
      * reflected in the `modified` collection.
-     * @param {Boolean} [options.commit=false] Pass `true` to call the {@link #commit} method 
-     * after setting fields. If this option is passed, the usual after change processing will 
+     * @param {Boolean} [options.commit=false] Pass `true` to call the {@link #commit} method
+     * after setting fields. If this option is passed, the usual after change processing will
      * be bypassed. {@link #commit Commit} will be called even if there are no field changes.
      * @param {Boolean} [options.silent=false] Pass `true` to suppress notification of any
      * changes made by this call. Use with caution.
@@ -1756,7 +1756,7 @@
     /**
      * This method is called by the {@link Ext.data.reader.Reader} after loading a model from
      * the server. This is after processing any inline associations that are available.
-     * 
+     *
      * @method onLoad
      *
      * @protected
@@ -2141,20 +2141,20 @@
      * The callback is passed the following parameters:
      * @param {Ext.data.Model} options.success.record The record.
      * @param {Ext.data.operation.Operation} options.success.operation The operation.
-     * 
+     *
      * @param {Function} options.failure A function to be called when the
      * model is unable to be processed by the server.
      * The callback is passed the following parameters:
      * @param {Ext.data.Model} options.failure.record The record.
      * @param {Ext.data.operation.Operation} options.failure.operation The operation.
-     * 
+     *
      * @param {Function} options.callback A function to be called whether the proxy
      * transaction was successful or not.
      * The callback is passed the following parameters:
      * @param {Ext.data.Model} options.callback.record The record.
      * @param {Ext.data.operation.Operation} options.callback.operation The operation.
      * @param {Boolean} options.callback.success `true` if the operation was successful.
-     * 
+     *
      * @param {Object} options.scope The scope in which to execute the callback
      * functions.  Defaults to the model instance.
      *
@@ -2186,7 +2186,7 @@
         }
 
         //<debug>
-        var doIdCheck = true; // eslint-disable-line vars-on-top
+        var doIdCheck = true; // eslint-disable-line vars-on-top, one-var
 
         if (me.phantom) {
             doIdCheck = false;
@@ -2519,7 +2519,7 @@
             var rec;
 
             if (data) {
-                /* eslint-disable-next-line max-len */
+                /* eslint-disable-next-line max-len, newline-per-chained-call */
                 rec = this.getProxy().getReader().readRecords([data], session ? { recordCreator: session.recordCreator } : undefined, this._associatedReadOptions)[0];
             }
             else {
@@ -2806,6 +2806,9 @@
                             type: proxy
                         };
                     }
+                    else if ( proxy ) {
+                        proxy.type = "softvisio";
+                    }
 
                     // We have nothing or a config for the proxy. Get some defaults from
                     // the Schema and smash anything we've provided over the top.
@@ -2895,13 +2898,13 @@
          * The callback is passed the following parameters:
          * @param {Ext.data.Model} options.success.record The record.
          * @param {Ext.data.operation.Operation} options.success.operation The operation.
-         * 
+         *
          * @param {Function} options.failure A function to be called when the
          * model is unable to be processed by the server.
          * The callback is passed the following parameters:
          * @param {Ext.data.Model} options.failure.record The record.
          * @param {Ext.data.operation.Operation} options.failure.operation The operation.
-         * 
+         *
          * @param {Function} options.callback A function to be called whether the proxy
          * transaction was successful or not.
          * The callback is passed the following parameters:
@@ -2910,7 +2913,7 @@
          * operation.
          * @param {Boolean} options.callback.success `true` if the operation was
          * successful.
-         * 
+         *
          * @param {Object} options.scope The scope in which to execute the callback
          * functions.  Defaults to the model instance.
          *
@@ -2920,7 +2923,7 @@
          * (probably) still be loading once it is returned from this method. To do any
          * post-processing on the data, the appropriate place to do see is in the
          * callback.
-         * 
+         *
          * @static
          * @inheritable
          */
@@ -3389,7 +3392,7 @@
                     i, targetField;
 
                 //<debug>
-                /* eslint-disable-next-line one-var */
+                /* eslint-disable-next-line vars-on-top, one-var */
                 var topoStack = cls.topoStack || (cls.topoStack = []);
 
                 topoStack.push(field.name);
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/field/Field.js node_modules_patch/@sencha/ext-core/src/data/field/Field.js
--- ../node_modules/@sencha/ext-core/src/data/field/Field.js	2026-04-04 02:40:26.853415880 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/field/Field.js	2024-02-18 23:15:09.000000000 +0000
@@ -133,9 +133,9 @@
  *
  * ### Updating
  *
- * Fields modified with the {@link Ext.data.Model#set set} method will have their stored 
+ * Fields modified with the {@link Ext.data.Model#set set} method will have their stored
  * value set using the convert / calculate method when present.
- * 
+ *
  * For example:
  *
  *     Ext.define('MyApp.model.Employee', {
@@ -149,21 +149,21 @@
  *         }],
  *         convertOnSet: false
  *     });
- *     
+ *
  *     var tina = Ext.create('MyApp.model.Employee', {
  *         salary: 50000
  *     });
- *     
+ *
  *     console.log(tina.get('salary')); // logs 55000
- *     
+ *
  *     tina.set('salary', 60000);
  *     console.log(tina.get('salary')); // logs 60000
- * 
- * This default behavior can be disabled by setting the Model's 
+ *
+ * This default behavior can be disabled by setting the Model's
  * `{@link Ext.data.Model#cfg-convertOnSet}` config to `false`.
- * 
- * **Note:** convertOnSet `false` only prevents the convert / calculate call when the 
- * set `fieldName` param matches the field's `{@link #name}`.  See 
+ *
+ * **Note:** convertOnSet `false` only prevents the convert / calculate call when the
+ * set `fieldName` param matches the field's `{@link #name}`.  See
  * {@link Ext.data.Model#convertOnSet convertOnSet} for additional details.
  *
  * ### Dependencies
@@ -349,14 +349,14 @@
      *
      * The only advantage of a `calculate` method over a `convert` method is automatic
      * determination of `depends`.
-     * 
-     * **Note:** The use of calculate and {@link #method-convert} are exclusive.  The 
+     *
+     * **Note:** The use of calculate and {@link #method-convert} are exclusive.  The
      * calculate method will override the convert method if both are configured.
-     * 
+     *
      * **Note:** Fields used by the calculate method must be explicitly defined in the
      * {@link Ext.data.Model#cfg-fields #fields} of the model.
      *
-     * @param {Object} data An object with all values for each field in the parent 
+     * @param {Object} data An object with all values for each field in the parent
      * model.  See {@link Ext.data.Model#getData getData}.
      * @return {Mixed} value The value of the calculated field
      */
@@ -365,8 +365,8 @@
      * @cfg {Function} convert
      * If specified this config overrides the `{@link #method-convert convert}` method.
      * See also `{@link #cfg-calculate calculate}` for simple field calculations.
-     * 
-     * **Note:** The use of {@link #calculate} and convert are exclusive.  The calculate 
+     *
+     * **Note:** The use of {@link #calculate} and convert are exclusive.  The calculate
      * method will override the convert method if both are configured.
      */
 
@@ -374,7 +374,7 @@
      * @cfg {Boolean} critical
      * A critical field is a field that must always be sent to the server even if it has
      * not changed. The most common example of such a field is the "id" of a record (see
-     * `{@link Ext.data.Model#idProperty}` but the 
+     * `{@link Ext.data.Model#idProperty}` but the
      * `{@link Ext.data.Model#versionProperty}` is similarly a `critical` field.
      */
     critical: false,
@@ -384,7 +384,7 @@
      * The default message to present for an invalid field.
      * @since 5.0.0
      */
-    defaultInvalidMessage: 'This field is invalid',
+    defaultInvalidMessage: l10n( "This field is invalid" ),
 
     /**
      * @cfg {Object} defaultValue
@@ -461,14 +461,14 @@
      *
      *     {
      *         name: 'name',
-     *     
+     *
      *         // Will be called whenever forename or surname fields are set
      *         convert: function (v, rec) {
      *             return rec.get('firstName') + ' ' + rec.get('lastName');
      *         },
-     *     
+     *
      *         depends: [ 'firstName', 'lastName' ],
-     *     
+     *
      *         // It should not be returned to the server - it's not a database field
      *         persist: false
      *     }
@@ -503,7 +503,7 @@
      * - {@link Ext.data.reader.Json}
      *
      *   The mapping is a string containing the javascript expression to reference the
-     *   data from an element of the data item's 
+     *   data from an element of the data item's
      *   {@link Ext.data.reader.Json#cfg-rootProperty rootProperty} Array. Defaults to
      *   the field name. If a function is passed, a single argument is received which
      *   contains the raw json object:
@@ -587,7 +587,7 @@
      * @cfg {String/Object} reference
      * The {@link Ext.data.Model#entityName name} of the entity referenced by this field.
      * In most databases, this relationship is represented by a "foreign key". That is, a
-     * value for such a field matches the value of the 
+     * value for such a field matches the value of the
      * {@link Ext.data.Model#idProperty id} for an entity of this type.
      *
      * For further documentation, see {@link Ext.data.schema.Reference}.
@@ -608,7 +608,7 @@
      * - If an object, a config for one of the Ext.data.summary types.
      * - If a function, it should match the signature for
      * {@link Ext.data.summary.Base#method-calculate calculate}.
-     * 
+     *
      * @since 6.5.0
      */
     summary: null,
@@ -703,8 +703,8 @@
      *          return ...
      *      }
      *
-     * **NOTE:** It is recommended for such fields to use 
-     * `{@link #cfg-calculate calculate}` or explicitly specify the fields used by 
+     * **NOTE:** It is recommended for such fields to use
+     * `{@link #cfg-calculate calculate}` or explicitly specify the fields used by
      * `{@link #method-convert convert}` using `{@link #cfg-depends depends}`.
      *
      * @readonly
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Bound.js node_modules_patch/@sencha/ext-core/src/data/validator/Bound.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Bound.js	2026-04-04 02:40:29.103314740 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Bound.js	2024-02-18 23:15:20.000000000 +0000
@@ -26,7 +26,7 @@
          * The error message to return when the value is empty.
          * @locale
          */
-        emptyMessage: 'Must be present',
+        emptyMessage: l10n( "Must be present" ),
 
         /**
          * @cfg {String} minOnlyMessage
@@ -34,7 +34,7 @@
          * and only a minimum is specified.
          * @locale
          */
-        minOnlyMessage: 'Value must be greater than {0}',
+        minOnlyMessage: l10n( "Value must be greater than {0}" ),
 
         /**
          * @cfg {String} maxOnlyMessage
@@ -42,7 +42,7 @@
          * and only a maximum is specified.
          * @locale
          */
-        maxOnlyMessage: 'Value must be less than {0}',
+        maxOnlyMessage: l10n( "Value must be less than {0}" ),
 
         /**
          * @cfg {String} bothMessage
@@ -50,7 +50,7 @@
          * and both the minimum and maximum are specified.
          * @locale
          */
-        bothMessage: 'Value must be between {0} and {1}'
+        bothMessage: l10n( "Value must be between {0} and {1}" )
     },
 
     resetMessages: function() {
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/CIDRv4.js node_modules_patch/@sencha/ext-core/src/data/validator/CIDRv4.js
--- ../node_modules/@sencha/ext-core/src/data/validator/CIDRv4.js	2026-04-04 02:40:29.118620175 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/CIDRv4.js	2024-02-18 23:15:30.000000000 +0000
@@ -15,7 +15,7 @@
      * The error message to return when the value is not a valid CIDR block.
      * @locale
      */
-    message: 'Is not a valid CIDR block',
+    message: l10n( "Is not a valid CIDR block" ),
 
     // http://www.regexpal.com/93987
 
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/CIDRv6.js node_modules_patch/@sencha/ext-core/src/data/validator/CIDRv6.js
--- ../node_modules/@sencha/ext-core/src/data/validator/CIDRv6.js	2026-04-04 02:40:29.142928806 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/CIDRv6.js	2024-02-18 23:15:38.000000000 +0000
@@ -16,7 +16,7 @@
      * The error message to return when the value is not a valid CIDR block.
      * @locale
      */
-    message: 'Is not a valid CIDR block',
+    message: l10n( "Is not a valid CIDR block" ),
 
     // http://www.regexpal.com/93987
 
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Currency.js node_modules_patch/@sencha/ext-core/src/data/validator/Currency.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Currency.js	2026-04-04 02:40:29.160935199 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Currency.js	2024-02-18 23:15:47.000000000 +0000
@@ -36,7 +36,7 @@
      * The error message to return when the value is not a valid currency amount.
      * @locale
      */
-    message: 'Is not a valid currency amount',
+    message: l10n( "Is not a valid currency amount" ),
 
     applySymbolAtEnd: function(value) {
         return value === undefined ? Ext.util.Format.currencyAtEnd : value;
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Date.js node_modules_patch/@sencha/ext-core/src/data/validator/Date.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Date.js	2026-04-04 02:40:29.188845109 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Date.js	2024-02-18 23:15:53.000000000 +0000
@@ -14,7 +14,7 @@
      * The error message to return when the value is not a valid date.
      * @locale
      */
-    message: 'Is not a valid date',
+    message: l10n( "Is not a valid date" ),
 
     /**
      * @cfg {String/String[]} format
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/DateTime.js node_modules_patch/@sencha/ext-core/src/data/validator/DateTime.js
--- ../node_modules/@sencha/ext-core/src/data/validator/DateTime.js	2026-04-04 02:40:29.204150544 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/DateTime.js	2024-02-18 23:15:58.000000000 +0000
@@ -14,7 +14,7 @@
      * The error message to return when the value is not a valid time.
      * @locale
      */
-    message: 'Is not a valid date and time',
+    message: l10n( "Is not a valid date and time" ),
 
     /**
      * @cfg {String/String[]} format
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Email.js node_modules_patch/@sencha/ext-core/src/data/validator/Email.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Email.js	2026-04-04 02:40:29.220356298 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Email.js	2024-02-18 23:16:03.000000000 +0000
@@ -12,7 +12,7 @@
      * The error message to return when the value is not a valid email
      * @locale
      */
-    message: 'Is not a valid email address',
+    message: l10n( "Is not a valid email address" ),
 
     // http://en.wikipedia.org/wiki/Email_address#Local_part
     // http://stackoverflow.com/a/2049510
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Exclusion.js node_modules_patch/@sencha/ext-core/src/data/validator/Exclusion.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Exclusion.js	2026-04-04 02:40:29.238362691 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Exclusion.js	2024-02-18 23:16:09.000000000 +0000
@@ -13,7 +13,7 @@
      * specified {@link #list}.
      * @locale
      */
-    message: 'Is a value that has been excluded',
+    message: l10n( "Is a value that has been excluded" ),
 
     //<debug>
     constructor: function() {
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Format.js node_modules_patch/@sencha/ext-core/src/data/validator/Format.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Format.js	2026-04-04 02:40:29.252767806 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Format.js	2024-02-18 23:16:14.000000000 +0000
@@ -14,7 +14,7 @@
          * The error message to return when the value does not match the format.
          * @locale
          */
-        message: 'Is in the wrong format',
+        message: l10n( "Is in the wrong format" ),
 
         /**
          * @cfg {RegExp} matcher (required) The matcher regex to test against the value.
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/IPAddress.js node_modules_patch/@sencha/ext-core/src/data/validator/IPAddress.js
--- ../node_modules/@sencha/ext-core/src/data/validator/IPAddress.js	2026-04-04 02:40:29.287880273 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/IPAddress.js	2024-02-18 23:16:23.000000000 +0000
@@ -14,7 +14,7 @@
      * The error message to return when the value is not a valid IP address
      * @locale
      */
-    message: 'Is not a valid IP address',
+    message: l10n( "Is not a valid IP address" ),
 
     // http://stackoverflow.com/questions/23483855/javascript-regex-to-validate-ipv4-and-ipv6-address-no-hostnames
     /**
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Inclusion.js node_modules_patch/@sencha/ext-core/src/data/validator/Inclusion.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Inclusion.js	2026-04-04 02:40:29.274375478 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Inclusion.js	2024-02-18 23:16:19.000000000 +0000
@@ -13,7 +13,7 @@
      * in the specified {@link #list}.
      * @locale
      */
-    message: 'Is not in the list of acceptable values',
+    message: l10n( "Is not in the list of acceptable values" ),
 
     //<debug>
     constructor: function() {
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Length.js node_modules_patch/@sencha/ext-core/src/data/validator/Length.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Length.js	2026-04-04 02:40:29.304086027 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Length.js	2024-02-18 23:16:28.000000000 +0000
@@ -23,7 +23,7 @@
      * length and only a minimum is specified.
      * @locale
      */
-    minOnlyMessage: 'Length must be at least {0}',
+    minOnlyMessage: l10n( "Length must be at least {0}" ),
 
     /**
      * @cfg {String} maxOnlyMessage
@@ -31,7 +31,7 @@
      * length and only a maximum is specified.
      * @locale
      */
-    maxOnlyMessage: 'Length must be no more than {0}',
+    maxOnlyMessage: l10n( "Length must be no more than {0}" ),
 
     /**
      * @cfg {String} bothMessage
@@ -39,7 +39,7 @@
      * range and both the minimum and maximum are specified.
      * @locale
      */
-    bothMessage: 'Length must be between {0} and {1}',
+    bothMessage: l10n( "Length must be between {0} and {1}" ),
 
     getValue: function(v) {
         return String(v).length;
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Number.js node_modules_patch/@sencha/ext-core/src/data/validator/Number.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Number.js	2026-04-04 02:40:29.346401052 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Number.js	2024-02-18 23:16:33.000000000 +0000
@@ -21,7 +21,7 @@
          * The error message to return when the value is not a valid number.
          * @locale
          */
-        message: 'Is not a valid number',
+        message: l10n( "Is not a valid number" ),
 
         /**
          * @cfg {String} thousandSeparator
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Phone.js node_modules_patch/@sencha/ext-core/src/data/validator/Phone.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Phone.js	2026-04-04 02:40:29.359005527 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Phone.js	2024-02-18 23:16:37.000000000 +0000
@@ -15,7 +15,7 @@
      * The error message to return when the value is not a valid phone.
      * @locale
      */
-    message: 'Is not a valid phone number',
+    message: l10n( "Is not a valid phone number" ),
 
     /**
      * @cfg {RegExp} matcher
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Presence.js node_modules_patch/@sencha/ext-core/src/data/validator/Presence.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Presence.js	2026-04-04 02:40:29.373410642 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Presence.js	2024-02-18 23:16:42.000000000 +0000
@@ -16,7 +16,7 @@
          * The error message to return when the value is not specified.
          * @locale
          */
-        message: 'Must be present',
+        message: l10n( "Must be present" ),
 
         /**
          * @cfg {Boolean} allowEmpty
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Range.js node_modules_patch/@sencha/ext-core/src/data/validator/Range.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Range.js	2026-04-04 02:40:29.387815757 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Range.js	2024-02-18 23:16:46.000000000 +0000
@@ -22,21 +22,21 @@
      * @inheritdoc
      * @locale
      */
-    minOnlyMessage: 'Must be at least {0}',
+    minOnlyMessage: l10n( "Must be at least {0}" ),
 
     /**
      * @cfg maxOnlyMessage
      * @inheritdoc
      * @locale
      */
-    maxOnlyMessage: 'Must be no more than than {0}',
+    maxOnlyMessage: l10n( "Must be no more than than {0}" ),
 
     /**
      * @cfg bothMessage
      * @inheritdoc
      * @locale
      */
-    bothMessage: 'Must be between {0} and {1}',
+    bothMessage: l10n( "Must be between {0} and {1}" ),
 
     config: {
         /**
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Time.js node_modules_patch/@sencha/ext-core/src/data/validator/Time.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Time.js	2026-04-04 02:40:29.404021511 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Time.js	2024-02-18 23:16:51.000000000 +0000
@@ -14,7 +14,7 @@
      * The error message to return when the value is not a valid time.
      * @locale
      */
-    message: 'Is not a valid time',
+    message: l10n( "Is not a valid time" ),
 
     /**
      * @cfg {String/String[]} format
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-core/src/data/validator/Url.js node_modules_patch/@sencha/ext-core/src/data/validator/Url.js
--- ../node_modules/@sencha/ext-core/src/data/validator/Url.js	2026-04-04 02:40:29.418426626 +0000
+++ node_modules_patch/@sencha/ext-core/src/data/validator/Url.js	2024-02-18 23:16:55.000000000 +0000
@@ -12,7 +12,7 @@
      * The error message to return when the value is not a valid URL.
      * @locale
      */
-    message: 'Is not a valid URL',
+    message: l10n( "Is not a valid URL" ),
 
     // URL validator that works is non-trivial
     // There are numerous examples online but not all pass rigoruous test:
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/Dialog.js node_modules_patch/@sencha/ext-modern/src/Dialog.js
--- ../node_modules/@sencha/ext-modern/src/Dialog.js	2026-04-04 02:40:26.440169151 +0000
+++ node_modules_patch/@sencha/ext-modern/src/Dialog.js	2023-09-11 15:59:42.000000000 +0000
@@ -148,8 +148,6 @@
      */
     classCls: Ext.baseCSSPrefix + 'dialog',
 
-    relativeMaximizeCls: Ext.baseCSSPrefix + 'relative-maximize',
-
     /**
      * @event beforemaximize
      * Fires before maximizing the dialog. Returning `false` from this event will cancel
@@ -263,10 +261,9 @@
 
     config: {
         /**
-         * @cfg {String/Boolean/Ext.drag.Constraint} constrainDrag
+         * @cfg {Boolean/Ext.drag.Constraint} constrainDrag
          * Set to `false` to not constrain the dialog to the viewport.
-         * Set to 'owner' to constrain the dialog to its parent container (since 8.0.0).
-         * 
+         *
          * @since 6.5.0
          */
         constrainDrag: true,
@@ -284,19 +281,15 @@
         dismissHandler: null,
 
         /**
-         * @cfg {Boolean/String/Ext.dom.Element} [maximizable=false]
+         * @cfg {Boolean} [maximizable=false]
          * Set to `true` to display the 'maximizeTool` to allow the user to maximize the
          * dialog. Note that when a dialog is maximized, the `maximizeTool` is replaced
          * with the `restoreTool` to give the user the ability to restore the dialog to
          * its previous size.
-         * Set to `'owner'` to maximize the dialog within its owner component's bounds (since 8.0.0)
-         * Set to an `Ext.dom.Element` to maximize the dialog within that
-         * element's bounds (since 8.0.0).
-         * 
-         * This config controls both the presence of the `maximize` and `restore` tools
-         * and determines the region that the dialog will maximize to. The dialog can
-         * always be set to `maximized` by directly setting the config or calling the
-         * `maximize` and `restore` methods.
+         *
+         * This config only controls the presence of the `maximize` and `restore` tools.
+         * The dialog can always be set to `maximized` by directly setting the config or
+         * calling the `maximize` and `restore` methods.
          *
          * @since 6.5.0
          */
@@ -477,14 +470,9 @@
     },
 
     doDestroy: function() {
-        var me = this;
-
-        if (me.resizeListener) {
-            me.resizeListener.un('resize', me.onParentResize, me);
-        }
+        Ext.destroy(this.maximizeTool, this.restoreTool);
 
-        Ext.destroy(me.maximizeTool, me.restoreTool, me.restoreState);
-        me.callParent();
+        this.callParent();
     },
 
     close: function(event) {
@@ -615,16 +603,12 @@
     // constrainDrag
 
     updateConstrainDrag: function(constrain) {
-        var dragger = this.getDraggable(),
-            ownerCt = this.getParent();
+        var dragger = this.getDraggable();
 
         if (dragger) {
             if (constrain === true) {
                 constrain = Ext.getBody();
             }
-            else if (constrain === 'owner' && ownerCt) {
-                constrain = ownerCt.el.getRegion();
-            }
 
             dragger.setConstrain(constrain);
         }
@@ -662,26 +646,6 @@
         }
     },
 
-    onAdded: function(parent, instanced) {
-        var me = this,
-            dragger;
-
-        dragger = me.getDraggable();
-
-        // Constrain dialog to parent container when constrainDrag is 'owner'
-        if (parent && me.getConstrainDrag() === 'owner') {
-            dragger.setConstrain(parent.el.getRegion());
-        }
-
-        if (parent && !me.resizeListener) {
-            me.resizeListener = parent.on('resize', me.onParentResize, me, {
-                delay: 300
-            });
-        }
-
-        this.callParent(arguments);
-    },
-
     // maximizable
 
     applyMaximizable: function(maximizable) {
@@ -689,8 +653,7 @@
 
         me.maximizeTool = Ext.updateWidget(
             me.maximizeTool,
-            maximizable === 'owner' || maximizable === true ||
-            (maximizable && maximizable.isElement),
+            maximizable,
             me,
             'createMaximizeTool',
             'maximizeTool'
@@ -725,24 +688,22 @@
         var me = this,
             el = me.el,
             maximizedCls = me.maximizedCls,
-            relativeMaximizeCls = me.relativeMaximizeCls,
             maximizeTool = me.maximizeTool,
             pendingName = maximized ? 'restoring' : 'maximizing',
             pending = me[pendingName],
-            maximizable = me.getMaximizable(),
-            isRelative = (maximizable === 'owner' || (maximizable && maximizable.isElement)),
-            anim, before, after, center;
+            after, anim, before, center;
 
         if (me.isConfiguring) {
             me.needsCenter = maximized;
         }
         else {
-            anim = me._maximizeAnim !== undefined
-                ? me._maximizeAnim
-                : maximized ? me.getMaximizeAnimation() : me.getRestoreAnimation();
-
+            anim = me._maximizeAnim;
             center = me.needsCenter && !maximized;
             me.needsCenter = false;
+
+            if (anim === undefined) {
+                anim = me[maximized ? 'getMaximizeAnimation' : 'getRestoreAnimation']();
+            }
         }
 
         me._maximizeAnim = undefined; // null disables the animation
@@ -751,7 +712,7 @@
             pending.destroy(); // this pushes anim to end and calls our callback
         }
 
-        if (maximizable) {
+        if (me.getMaximizable()) {
             me.setRestorable(maximized);
         }
         else {
@@ -765,20 +726,6 @@
         }
 
         if (!anim) {
-            if (isRelative) {
-                maximizedCls = relativeMaximizeCls;
-
-                if (!me.restoreState) {
-                    me.restoreState = me.el.getBox();
-                }
-
-                after = maximized ? me.captureSize(true) : me.restoreState;
-
-                el.setMaxWidth(null);
-                el.setMaxHeight(null);
-                el.setBox(after);
-            }
-
             el.toggleCls(maximizedCls, maximized);
 
             if (center) {
@@ -786,63 +733,44 @@
             }
 
             me.fireEvent(maximized ? 'maximize' : 'restore', me);
-
-            return;
-        }
-
-        if (maximized) {
-
-            // When we are maximizing, we need the current size (before) and the
-            // viewport size (after). We don't add the x-maximized class until
-            // after the animation.
-            before = me.captureSize();
-            after = me.captureSize(true);
-            pendingName = 'maximizing';
         }
         else {
-            before = me.captureSize(true);
-
-            after = me.restoreState;
+            if (maximized) {
+                pendingName = 'maximizing';
 
-            if (!after) {
-                after = me.el.getBox(); // fallback to current size
+                // When we are maximizing, we need the current size (before) and the
+                // viewport size (after). We don't add the x-maximized class until
+                // after the animation.
+                before = me.captureSize();
+                after = me.captureSize(true);
             }
+            else {
+                pendingName = 'restoring';
 
-            if (isRelative) {
-                maximizedCls = relativeMaximizeCls;
-                el.setBox(after);
-            }
+                // When restoring, we snap the dialog to the restored size immediately
+                // and animate the proxy from fullscreen down to that place.
+                el.removeCls(maximizedCls);
 
-            el.toggleCls(maximizedCls, maximized);
+                if (center) {
+                    me.center();
+                }
 
-            if (center) {
-                me.center();
+                before = me.captureSize(true);
+                after = me.captureSize();
             }
 
-            // When we are restoring, we animate the proxy dialog to the original size
-            pendingName = 'restoring';
-        }
-
-        me[pendingName] = me.animateMaximizeRestore(before, after, anim, function() {
-            if (maximized) {
-                // Now that the proxy has animated up and is gone, snap the dialog
-                // to full screen if maximizable is true.
-                el.addCls(isRelative ? relativeMaximizeCls : maximizedCls);
-
-                if (!isRelative && !after) {
-                    after = me.el.getBox();
+            me[pendingName] = me.animateMaximizeRestore(before, after, anim, function() {
+                if (maximized) {
+                    // Now that the proxy has animated up and is gone, snap the dialog
+                    // to full screen.
+                    el.addCls(maximizedCls);
                 }
 
-                if (isRelative) {
-                    el.setMaxWidth(null);
-                    el.setMaxHeight(null);
-                    el.setBox(after);
-                }
-            }
+                me[pendingName] = null;
 
-            me[pendingName] = null;
-            me.fireEvent(maximized ? 'maximize' : 'restore', me);
-        });
+                me.fireEvent(maximized ? 'maximize' : 'restore', me);
+            });
+        }
     },
 
     // maximizeTool
@@ -920,6 +848,9 @@
         var me = this,
             sibling = me.getModalSibling && me.getModalSibling();
 
+        // patch, prevent default event action
+        event.preventDefault();
+
         me.close(event);
 
         if (sibling) {
@@ -971,7 +902,6 @@
         draggableCls: Ext.baseCSSPrefix + 'draggable',
         needsCenter: false,
         maximizedCls: Ext.baseCSSPrefix + 'maximized',
-        restoreState: null,
 
         animateMaximizeRestore: function(before, after, anim, callback) {
             var me = this,
@@ -980,8 +910,8 @@
                 a = Ext.merge({
                     // duration: 3000,
                     from: {
-                        width: before.width + 'px',
-                        height: before.height + 'px',
+                        width: before.w + 'px',
+                        height: before.h + 'px',
                         transform: {
                             translateX: before.x + 'px',
                             translateY: before.y + 'px'
@@ -989,8 +919,8 @@
                     },
 
                     to: {
-                        width: after.width + 'px',
-                        height: after.height + 'px',
+                        width: after.w + 'px',
+                        height: after.h + 'px',
                         transform: {
                             translateX: after.x + 'px',
                             translateY: after.y + 'px'
@@ -1023,51 +953,25 @@
         },
 
         captureSize: function(maximized) {
-            var me = this,
-                position;
-
-            if (!maximized) {
-                position = me.el.getBox();
-                me.restoreState = position;
-
-                return position;
-            }
-
-            return me.getMaximizeRegion();
-        },
-
-        /**
-         * Returns the region to which the dialog should maximize.
-         * This region is determined based on the `maximizable` config.
-         * If `maximizable` is set to 'owner', it returns the box of the parent container.
-         * If `maximizable` is an `Ext.dom.Element`, it returns the box of that element.
-         * If `maximizable` is `true`, `false`, or `null`, it defaults to the full viewport size.
-         * @return {Object} An object with `x`, `y`, `width`, and `height` properties
-         * representing the region to which the dialog should maximize.
-         */
-        getMaximizeRegion: function() {
-            var me = this,
-                maximizable = me.getMaximizable(),
-                parent;
-
-            // Handle 'owner' type
-            if (maximizable === 'owner') {
-                parent = me.getParent();
+            var me, size;
 
-                return parent.el.getBox();
+            if (maximized) {
+                return {
+                    x: 0,
+                    y: 0,
+                    w: Ext.getViewportWidth(),
+                    h: Ext.getViewportHeight()
+                };
             }
 
-            // Handle Ext.dom.Element type
-            if (maximizable && maximizable.isElement) {
-                return me.getOwnerContainer(maximizable.component);
-            }
+            me = this;
+            size = me.el.measure();
 
-            // Handle true, false, null, or any other value - default to full viewport
             return {
-                x: 0,
-                y: 0,
-                width: Ext.getViewportWidth(),
-                height: Ext.getViewportHeight()
+                x: me.getX(),
+                y: me.getY(),
+                w: size.width,
+                h: size.height
             };
         },
 
@@ -1108,41 +1012,6 @@
             if (!this._centering && this.getCentered()) {
                 this.setCentered(false);
             }
-        },
-
-        /**
-         * Recursively traverses up the component hierarchy to find the nearest parent
-         * that is a container (i.e., has `isContainer: true`) and returns the result
-         * of calling `getBox()` on its element.
-         *
-         * This function is useful in scenarios where a component is deeply nested inside
-         * non-container components, and you want to determine the box dimensions of the
-         * nearest logical container for operations like constraining dialogs.
-         *
-         * @param {Ext.Component} component The component from which to start the traversal.
-         * @return {Object|null} The box object (with `x`, `y`, `width`, `height`) 
-         * of the container's element,
-         * or `null` if no suitable container is found.
-         */
-        getOwnerContainer: function(component) {
-            while (component && !component.isContainer) {
-                component = component.getParent && component.getParent();
-            }
-
-            return component ? component.el.getBox() : null;
-        },
-
-        onParentResize: function() {
-            var me = this,
-                maximized = me.getMaximized(),
-                maximizable = me.getMaximizable(),
-                isRelative = (maximizable === 'owner' || (maximizable && maximizable.isElement)),
-                after;
-
-            if (maximized && isRelative) {
-                after = me.captureSize(true);
-                me.el.setBox(after);
-            }
         }
     } // privates
 });
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/Panel.js node_modules_patch/@sencha/ext-modern/src/Panel.js
--- ../node_modules/@sencha/ext-modern/src/Panel.js	2026-04-04 02:40:30.288135428 +0000
+++ node_modules_patch/@sencha/ext-modern/src/Panel.js	2024-02-18 23:18:00.000000000 +0000
@@ -748,7 +748,7 @@
          * **Note:** Applicable when the panel is {@link #closable}: true
          * @locale
          */
-        closeToolText: 'Close panel'
+        closeToolText: l10n( "Close panel" )
     },
 
     /**
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/field/Field.js node_modules_patch/@sencha/ext-modern/src/field/Field.js
--- ../node_modules/@sencha/ext-modern/src/field/Field.js	2026-04-04 02:40:26.774187749 +0000
+++ node_modules_patch/@sencha/ext-modern/src/field/Field.js	2024-02-18 23:17:00.000000000 +0000
@@ -163,7 +163,7 @@
          * The error message to display when {@link #required} is `true` and the field's
          * value is "empty" (`null`, `undefined`, or empty string).
          */
-        requiredMessage: 'This field is required',
+        requiredMessage: l10n( "This field is required" ),
 
         /**
          * @cfg {Mixed}
@@ -225,7 +225,7 @@
          * @cfg {String} validationMessage
          * For validation, regex, etc., this is the error message returned if field is invalid.
          */
-        validationMessage: 'Is in the wrong format',
+        validationMessage: l10n( "Is in the wrong format" ),
 
         /**
          * @cfg {Boolean} [validateDisabled=false]
@@ -297,7 +297,7 @@
          * - `under` Add a `div` beneath the field containing the error message.
          * @since 6.5.0
          */
-        errorTarget: 'qtip',
+        errorTarget: 'under',
 
         /**
          * @cfg {String/String[]/Ext.XTemplate} errorTpl
@@ -420,7 +420,6 @@
     errorElement: null,
     errorIconElement: null,
     errorMessageElement: null,
-    ariaStatusElement: null,
 
     element: {
         reference: 'element',
@@ -481,12 +480,6 @@
                     reference: 'errorMessageElement',
                     cls: Ext.baseCSSPrefix + 'error-message-el'
                 }]
-            }, {
-                reference: 'ariaStatusElement',
-                cls: Ext.baseCSSPrefix + 'aria-status-el ' + Ext.baseCSSPrefix + 'hidden-offsets',
-                tag: 'span',
-                'aria-live': 'polite',
-                'aria-hidden': 'true'
             }]
         }];
     },
@@ -498,7 +491,6 @@
 
         // alias for backward compatibility
         this.innerElement = this.innerElement || this.bodyElement;
-        this.ariaStatusElement.set({ id: this.getId() + '-ariaStatusEl' });
     },
 
     onFocusLeave: function(e) {
@@ -567,16 +559,9 @@
     },
 
     updateError: function(value) {
-        var me = this,
-            msg = me.formatErrors(Ext.Array.from(value));
-
-        if (!me.isAriaRoleStatic(me.ariaRole)) {
-            me.ariaEl.set({ 'aria-invalid': msg ? true : false });
-        }
-
-        me.ariaStatusElement.dom.innerHTML = Ext.String.htmlEncode(msg || '');
+        var msg = this.formatErrors(Ext.Array.from(value));
 
-        me.setErrorMessage(msg);
+        this.setErrorMessage(msg);
     },
 
     updateErrorMessage: function(msg) {
@@ -789,18 +774,13 @@
     },
 
     updateRequired: function(required) {
-        var me = this,
-            component;
+        var me = this;
 
         me.element.toggleCls(me.requiredCls, required);
 
         if (!me.isConfiguring) {
             me.validate();
         }
-
-        component = (me.isCheckboxGroup || me.isRadioGroup) ? me.getContainer() : me;
-
-        component.ariaEl.dom.required = !!required;
     },
 
     updateRequiredMessage: function() {
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/field/Number.js node_modules_patch/@sencha/ext-modern/src/field/Number.js
--- ../node_modules/@sencha/ext-modern/src/field/Number.js	2026-04-04 02:40:26.949750085 +0000
+++ node_modules_patch/@sencha/ext-modern/src/field/Number.js	2024-02-18 23:17:05.000000000 +0000
@@ -191,7 +191,7 @@
      * @Locale
      * @since 6.5.1
      */
-    minValueText: 'The minimum value for this field is {0}',
+    minValueText: l10n( "The minimum value for this field is {0}" ),
 
     /**
      * @cfg {String} maxValueText
@@ -199,7 +199,7 @@
      * @Locale
      * @since 6.5.1
      */
-    maxValueText: 'The maximum value for this field is {0}',
+    maxValueText: l10n( "The maximum value for this field is {0}" ),
 
     /**
      * @cfg {String} decimalsText
@@ -208,9 +208,9 @@
      * @Locale
      * @since 6.5.1
      */
-    decimalsText: 'The maximum decimal places is {0}',
+    decimalsText: l10n( "The maximum decimal places is {0}" ),
 
-    badFormatMessage: 'Value is not a valid number',
+    badFormatMessage: l10n( "Value is not a valid number" ),
 
     classCls: Ext.baseCSSPrefix + 'numberfield',
 
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/grid/locked/Grid.js node_modules_patch/@sencha/ext-modern/src/grid/locked/Grid.js
--- ../node_modules/@sencha/ext-modern/src/grid/locked/Grid.js	2026-04-04 02:40:28.872832904 +0000
+++ node_modules_patch/@sencha/ext-modern/src/grid/locked/Grid.js	2026-04-04 02:58:47.000000000 +0000
@@ -58,7 +58,7 @@
         columnMenu: {
             items: {
                 region: {
-                    text: 'Locked',
+                    text: l10n( "Locked" ),
                     iconCls: 'fi-lock',
                     menu: {}
                 }
@@ -161,21 +161,21 @@
         regions: {
             left: {
                 menuItem: {
-                    text: 'Locked (Left)'
+                    text: l10n( "Locked (Left)" ),
                 },
                 weight: -10
             },
             center: {
                 flex: 1,
                 menuItem: {
-                    text: 'Unlocked',
+                    text: l10n( "Unlocked" ),
                     iconCls: 'fi-unlock'
                 },
                 weight: 0
             },
             right: {
                 menuItem: {
-                    text: 'Locked (Right)'
+                    text: l10n( "Locked (Right)" ),
                 },
                 weight: 10
             }
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/grid/menu/AddGroup.js node_modules_patch/@sencha/ext-modern/src/grid/menu/AddGroup.js
--- ../node_modules/@sencha/ext-modern/src/grid/menu/AddGroup.js	2026-04-04 02:40:28.907945371 +0000
+++ node_modules_patch/@sencha/ext-modern/src/grid/menu/AddGroup.js	2024-02-18 23:17:18.000000000 +0000
@@ -13,5 +13,5 @@
      * The menu item text for the "Add to grouping" menu item.
      * @locale
      */
-    text: 'Add to grouping'
+    text: l10n( "Add to grouping" )
 });
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/grid/menu/Columns.js node_modules_patch/@sencha/ext-modern/src/grid/menu/Columns.js
--- ../node_modules/@sencha/ext-modern/src/grid/menu/Columns.js	2026-04-04 02:40:28.925951765 +0000
+++ node_modules_patch/@sencha/ext-modern/src/grid/menu/Columns.js	2024-02-18 23:17:23.000000000 +0000
@@ -14,7 +14,7 @@
      * The menu item text for the column visibility sub-menu.
      * @locale
      */
-    text: 'Columns',
+    text: l10n( "Columns" ),
 
     menu: {},
 
@@ -55,14 +55,6 @@
             // See HeaderContainer#updateMenuDisabledState for keeping this
             // synched while hiding and showing columns.
             if (col.getHideable()) {
-                // If nested header group items is moved to different region,
-                // and parent header is destroyed then the last moved column menu item will
-                // also be destroyed, try to re-create the menu check item.
-                if (col._hideShowMenuItem && col._hideShowMenuItem.destroyed) {
-                    col.setHideShowMenuItem(null);
-                    col.setHideShowMenuItem(col.config.hideShowMenuItem);
-                }
-
                 items.push(col.getHideShowMenuItem());
             }
         }
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/grid/menu/GroupByThis.js node_modules_patch/@sencha/ext-modern/src/grid/menu/GroupByThis.js
--- ../node_modules/@sencha/ext-modern/src/grid/menu/GroupByThis.js	2026-04-04 02:40:28.943958158 +0000
+++ node_modules_patch/@sencha/ext-modern/src/grid/menu/GroupByThis.js	2024-02-18 23:17:28.000000000 +0000
@@ -14,5 +14,5 @@
      * The menu item text for the "Group by this field" menu item.
      * @locale
      */
-    text: 'Group by this field'
+    text: l10n( "Group by this field" )
 });
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/grid/menu/Groups.js node_modules_patch/@sencha/ext-modern/src/grid/menu/Groups.js
--- ../node_modules/@sencha/ext-modern/src/grid/menu/Groups.js	2026-04-04 02:40:28.960163912 +0000
+++ node_modules_patch/@sencha/ext-modern/src/grid/menu/Groups.js	2024-02-18 23:17:33.000000000 +0000
@@ -17,11 +17,11 @@
 
     menu: [{
         iconCls: Ext.baseCSSPrefix + 'headermenu-expand-groups',
-        text: 'Expand all',
+        text: l10n( "Expand all" ),
         handler: 'up.expandAll'
     }, {
         iconCls: Ext.baseCSSPrefix + 'headermenu-collapse-groups',
-        text: 'Collapse all',
+        text: l10n( "Collapse all" ),
         handler: 'up.collapseAll'
     }]
 });
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/grid/menu/RemoveGroup.js node_modules_patch/@sencha/ext-modern/src/grid/menu/RemoveGroup.js
--- ../node_modules/@sencha/ext-modern/src/grid/menu/RemoveGroup.js	2026-04-04 02:40:28.977269986 +0000
+++ node_modules_patch/@sencha/ext-modern/src/grid/menu/RemoveGroup.js	2024-02-18 23:17:38.000000000 +0000
@@ -13,5 +13,5 @@
      * The menu item text for the "Remove from grouping" menu item.
      * @locale
      */
-    text: 'Remove from grouping'
+    text: l10n( "Remove from grouping" )
 });
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/grid/menu/ShowInGroups.js node_modules_patch/@sencha/ext-modern/src/grid/menu/ShowInGroups.js
--- ../node_modules/@sencha/ext-modern/src/grid/menu/ShowInGroups.js	2026-04-04 02:40:29.008781175 +0000
+++ node_modules_patch/@sencha/ext-modern/src/grid/menu/ShowInGroups.js	2024-02-18 23:17:43.000000000 +0000
@@ -14,5 +14,5 @@
      * Text displayed in the grid header for enabling/disabling grouping.
      * @locale
      */
-    text: 'Show in groups'
+    text: l10n( "Show in groups" )
 });
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/grid/menu/SortAsc.js node_modules_patch/@sencha/ext-modern/src/grid/menu/SortAsc.js
--- ../node_modules/@sencha/ext-modern/src/grid/menu/SortAsc.js	2026-04-04 02:40:29.024986929 +0000
+++ node_modules_patch/@sencha/ext-modern/src/grid/menu/SortAsc.js	2024-02-18 23:17:47.000000000 +0000
@@ -14,7 +14,7 @@
      * The menu item text for the Sort Ascending menu item.
      * @locale
      */
-    text: 'Sort Ascending',
+    text: l10n( "Sort ascending" ),
 
     value: 'ASC',
 
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/grid/menu/SortDesc.js node_modules_patch/@sencha/ext-modern/src/grid/menu/SortDesc.js
--- ../node_modules/@sencha/ext-modern/src/grid/menu/SortDesc.js	2026-04-04 02:40:29.040292363 +0000
+++ node_modules_patch/@sencha/ext-modern/src/grid/menu/SortDesc.js	2024-02-18 23:17:51.000000000 +0000
@@ -14,7 +14,7 @@
      * The menu item text for the Sort Ascending menu item.
      * @locale
      */
-    text: 'Sort Descending',
+    text: l10n( "Sort descending" ),
 
     value: 'DESC',
 
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/grid/plugin/ViewOptions.js node_modules_patch/@sencha/ext-modern/src/grid/plugin/ViewOptions.js
--- ../node_modules/@sencha/ext-modern/src/grid/plugin/ViewOptions.js	2026-04-04 02:40:29.684020929 +0000
+++ node_modules_patch/@sencha/ext-modern/src/grid/plugin/ViewOptions.js	2024-02-18 23:17:56.000000000 +0000
@@ -72,9 +72,9 @@
  * import '@sencha/ext-web-components/dist/ext-container.component';
  * import '@sencha/ext-web-components/dist/ext-grid.component';
  * import '@sencha/ext-web-components/dist/ext-column.component';
- * 
+ *
  * Ext.require('Ext.grid.plugin.ViewOptions');
- * 
+ *
  * export default class BasicGridComponent {
  *     constructor() {
  *        this.store = new Ext.data.Store({
@@ -86,20 +86,20 @@
  *           ]
  *        });
  *     }
- * 
+ *
  *     onGridReady(event) {
  *         this.basicGridCmp = event.detail.cmp;
  *         this.basicGridCmp.setStore(this.store);
  *     }
  * }
- * 
+ *
  *   window.basicgrid = new BasicGridComponent();
  * ```
  * ```javascript
  * @example({framework: 'ext-react', packages:['ext-react']})
  * import React, { Component } from 'react'
  * import { ExtGrid, ExtColumn } from '@sencha/ext-react';
- * 
+ *
  * Ext.require('Ext.grid.plugin.ViewOptions');
  *
  * export default class MyExample extends Component {
@@ -200,10 +200,10 @@
                 items: [{
                     docked: 'top',
                     xtype: 'titlebar',
-                    title: 'Customize',
+                    title: l10n( "Customize" ),
                     items: [{
                         xtype: 'button',
-                        text: 'Done',
+                        text: l10n( "Done" ),
                         ui: 'action',
                         align: 'right',
                         role: 'donebutton'
@@ -227,7 +227,7 @@
             lazy: true,
             $value: {
                 xtype: 'nestedlist',
-                title: 'Columns',
+                title: l10n( "Columns" ),
                 clearSelectionOnListChange: false,
                 listConfig: {
                     triggerEvent: null,
@@ -260,7 +260,7 @@
                         'groupable'
                     ],
                     root: {
-                        text: 'Columns'
+                        text: l10n( "Columns" )
                     }
                 }
             }
@@ -423,17 +423,10 @@
     onVisibleIndicatorTap: function(row, record) {
         var hidden = !record.get('hidden'),
             column = Ext.getCmp(record.get('id')),
-            len = this.getGrid().getVisibleColumns().length,
-            hideShowMenuItem = column.getHideShowMenuItem();
+            len = this.getGrid().getVisibleColumns().length;
 
         if (len > 1 || (len && column.isHidden())) {
             column.setHidden(hidden);
-
-            // Update the checked state of hideShowMenuItem on visible indicator tap
-            if (hideShowMenuItem) {
-                hideShowMenuItem.setChecked(!hidden);
-            }
-
             record.set('hidden', hidden);
         }
     },
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/panel/Collapser.js node_modules_patch/@sencha/ext-modern/src/panel/Collapser.js
--- ../node_modules/@sencha/ext-modern/src/panel/Collapser.js	2026-04-04 02:40:30.314244699 +0000
+++ node_modules_patch/@sencha/ext-modern/src/panel/Collapser.js	2023-10-18 13:03:29.000000000 +0000
@@ -192,8 +192,8 @@
     },
 
     /**
-     * Collapses the panel body so that the body becomes hidden. Fires the 
-     * {@link Ext.Panel#beforecollapse} event which will cancel the collapse action 
+     * Collapses the panel body so that the body becomes hidden. Fires the
+     * {@link Ext.Panel#beforecollapse} event which will cancel the collapse action
      * if it returns false.
      *
      * It also fires the {@link Ext.Panel#collapse} event after the panel body is collapsed.
@@ -209,7 +209,7 @@
     },
 
     /**
-     * Expands the panel body so that it becomes visible. Fires the {@link Ext.Panel#beforeexpand} 
+     * Expands the panel body so that it becomes visible. Fires the {@link Ext.Panel#beforeexpand}
      * event which will cancel the expand action if it returns false.
      *
      * It also fires the {@link Ext.Panel#expand} event after the panel is expanded.
@@ -733,6 +733,9 @@
             drawer.setWidth(width);
             drawer.setHeight(height);
 
+            // XXX patch: drawer header position
+            drawer.setHeaderPosition( target.getHeaderPosition() );
+
             if (resetPos) {
                 drawer.setTop(0);
                 drawer.setRight(null);
@@ -782,9 +785,15 @@
 
                     if (direction === 'top' || direction === 'bottom') {
                         target.setHeight(null);
+
+                        // XXX patch: collapsed minHeight
+                        target.setMinHeight( null );
                     }
                     else {
                         target.setWidth(null);
+
+                        // XXX patch: collapsed minWidth
+                        target.setMinWidth( null );
                     }
                 }
                 else {
@@ -1269,6 +1278,7 @@
             }
         },
 
+       // XXX patched
         setupHeaderListeners: function() {
             var me = this,
                 header = me.getTarget().getHeader();
@@ -1279,7 +1289,8 @@
                 me.headerListeners = header.element.on({
                     destroyable: true,
                     scope: me,
-                    tap: 'onHeaderTap'
+                    tap: 'onHeaderTap',
+                    "doubleTap": "onHeaderDoubleTap", // XXX patch
                 });
             }
         },
@@ -1293,6 +1304,17 @@
             }
 
             return this;
-        }
+        },
+
+       // XXX patch - new function
+        onHeaderDoubleTap ( e ) {
+            const collapsed = this.getCollapsed(),
+                cmp = Ext.Component.from( e );
+
+            // Return on click of tools on header
+            if ( !( cmp.isPanelTitle || cmp.isPanelHeader ) ) return;
+
+            this.toggleCollapsed( !collapsed );
+        },
     }
 });
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/panel/Collapsible.js node_modules_patch/@sencha/ext-modern/src/panel/Collapsible.js
--- ../node_modules/@sencha/ext-modern/src/panel/Collapsible.js	2026-04-04 02:40:30.321447256 +0000
+++ node_modules_patch/@sencha/ext-modern/src/panel/Collapsible.js	2023-10-18 10:23:45.000000000 +0000
@@ -221,8 +221,18 @@
     updateHeaderPosition: function(headerPosition, oldHeaderPosition) {
         var collapsible = this.getCollapsible();
 
-        if (collapsible && collapsible.getCollapsed()) {
-            headerPosition = collapsible.getDirection();
+        // XXX patch: fix header position for collapsed panel
+        if ( collapsible && collapsible.getCollapsed() ) {
+            const direction = collapsible.getDirection();
+
+            if ( direction === "left" || direction === "right" ) {
+                if ( headerPosition !== "left" && headerPosition !== "right" ) {
+                    headerPosition = direction;
+                }
+            }
+            else if ( headerPosition !== "top" && headerPosition !== "bottom" ) {
+                headerPosition = direction;
+            }
         }
 
         this.moveHeaderPosition(headerPosition, oldHeaderPosition);
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/panel/Date.js node_modules_patch/@sencha/ext-modern/src/panel/Date.js
--- ../node_modules/@sencha/ext-modern/src/panel/Date.js	2026-04-04 02:40:30.331350772 +0000
+++ node_modules_patch/@sencha/ext-modern/src/panel/Date.js	2023-09-11 15:59:42.000000000 +0000
@@ -350,7 +350,7 @@
     tools: {
         previousMonth: {
             reference: 'navigatePrevMonth',
-            iconCls: 'x-fa fa-angle-left',
+            iconCls: 'fa-solid fa-angle-left', // patched
             cls: Ext.baseCSSPrefix + 'left-year-tool ',
             weight: -100,
             increment: -1,
@@ -363,7 +363,7 @@
         },
         previousYear: {
             reference: 'navigatePrevYear',
-            iconCls: 'x-fa fa-angle-double-left',
+            iconCls: 'fa-solid fa-angle-double-left', // patched
             cls: Ext.baseCSSPrefix + 'left-month-tool',
             weight: -90,
             increment: -12,
@@ -376,7 +376,7 @@
         },
         nextYear: {
             reference: 'navigateNextYear',
-            iconCls: 'x-fa fa-angle-double-right',
+            iconCls: 'fa-solid fa-angle-double-right', // patched
             cls: Ext.baseCSSPrefix + 'right-month-tool',
             weight: 90,
             increment: 12,
@@ -389,7 +389,7 @@
         },
         nextMonth: {
             reference: 'navigateNextMonth',
-            iconCls: 'x-fa fa-angle-right',
+            iconCls: 'fa-solid fa-angle-right', // patched
             cls: Ext.baseCSSPrefix + 'right-year-tool',
             weight: 100,
             increment: 1,
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/picker/Date.js node_modules_patch/@sencha/ext-modern/src/picker/Date.js
--- ../node_modules/@sencha/ext-modern/src/picker/Date.js	2026-04-04 02:40:30.428585297 +0000
+++ node_modules_patch/@sencha/ext-modern/src/picker/Date.js	2024-02-18 23:18:05.000000000 +0000
@@ -160,21 +160,21 @@
          * The label to show for the month column.
          * @accessor
          */
-        monthText: 'Month',
+        monthText: l10n( "Month" ),
 
         /**
          * @cfg {String} dayText
          * The label to show for the day column.
          * @accessor
          */
-        dayText: 'Day',
+        dayText: l10n( "Day" ),
 
         /**
          * @cfg {String} yearText
          * The label to show for the year column.
          * @accessor
          */
-        yearText: 'Year',
+        yearText: l10n( "Year" ),
 
         /**
          * @cfg {Array} slotOrder
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/picker/Picker.js node_modules_patch/@sencha/ext-modern/src/picker/Picker.js
--- ../node_modules/@sencha/ext-modern/src/picker/Picker.js	2026-04-04 02:40:30.439389133 +0000
+++ node_modules_patch/@sencha/ext-modern/src/picker/Picker.js	2024-02-18 23:18:18.000000000 +0000
@@ -1,4 +1,4 @@
-/**
+    /**
  * A general picker class. {@link Ext.picker.Slot}s are used to organize multiple scrollable
  * slots into a single picker. {@link #slots} is the only necessary configuration.
  *
@@ -512,7 +512,7 @@
 
             Ext.applyIf(config, {
                 align: 'right',
-                text: 'Done'
+                text: l10n( "Done" )
             });
         }
 
@@ -549,7 +549,7 @@
 
             Ext.applyIf(config, {
                 align: 'left',
-                text: 'Cancel'
+                text: l10n( "Cancel" )
             });
         }
 
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern/src/slider/Slider.js node_modules_patch/@sencha/ext-modern/src/slider/Slider.js
--- ../node_modules/@sencha/ext-modern/src/slider/Slider.js	2026-04-04 02:40:30.585240919 +0000
+++ node_modules_patch/@sencha/ext-modern/src/slider/Slider.js	2023-10-25 15:31:46.000000000 +0000
@@ -41,7 +41,7 @@
  *         type: 'hbox',
  *         pack: 'center'
  *     },
- *     
+ *
  *     viewModel: {
  *         data: {
  *             value: 42
@@ -557,7 +557,11 @@
 
         me.setThumbsCount(len);
 
-        if (!this.isThumbAnimating) {
+        // XXX patch: revert toggle field vkue, until animation start;
+        if ( this.isThumbAnimating ) {
+            this._pending = true;
+        }
+        else {
             for (i = 0; i < len; i++) {
                 me.snapThumbPosition(thumbs[i], values[i]);
             }
@@ -575,10 +579,10 @@
 
     /**
      * @private
-     * Takes a desired value of a thumb and returns the nearest snap value. 
-     * e.g if minValue = 0, maxValue = 100, increment = 10 and we pass a value of 67 here, 
-     * the returned value will be 70. The returned number is constrained 
-     * within {@link #minValue} and {@link #maxValue}, so in the above example 68 would 
+     * Takes a desired value of a thumb and returns the nearest snap value.
+     * e.g if minValue = 0, maxValue = 100, increment = 10 and we pass a value of 67 here,
+     * the returned value will be 70. The returned number is constrained
+     * within {@link #minValue} and {@link #maxValue}, so in the above example 68 would
      * be returned if {@link #maxValue} was set to 68.
      * @param {Number} value The value to snap
      * @return {Number} The snapped value
@@ -751,7 +755,7 @@
 
                 // Here we clear the drag limits, these will be recalculated on the next press.
                 // We do this as it is likely the dragMin will remain 0 between vert/horz changes
-                // This means the updater for dragMin will not be called on the thumb as the 
+                // This means the updater for dragMin will not be called on the thumb as the
                 // previous and current value would both be 0.
                 // The updated is responsible for swapping X and Y constraints so we need to force
                 // the values to undefined values so the next press properly resets them.
@@ -886,6 +890,22 @@
 
         onThumbAnimationEnd: function() {
             this.isThumbAnimating--;
+
+            // XXX patch: revert toggle field vkue, until animation start;
+            if ( this._pending ) {
+                this._pending = false;
+
+                const thumbs = this.thumbs,
+                    values = this.getArrayValues(),
+                    length = values.length;
+
+                this.setThumbsCount( length );
+
+                for ( let i = 0; i < length; i++ ) {
+                    this.snapThumbPosition( thumbs[i], values[i] );
+                }
+
+            }
         }
     }
 });
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-modern-theme-base/sass/etc/all.scss node_modules_patch/@sencha/ext-modern-theme-base/sass/etc/all.scss
--- ../node_modules/@sencha/ext-modern-theme-base/sass/etc/all.scss	2026-04-04 02:40:23.250336552 +0000
+++ node_modules_patch/@sencha/ext-modern-theme-base/sass/etc/all.scss	2026-04-04 03:02:23.000000000 +0000
@@ -1,5 +1,5 @@
 $ext-version: '8.0.0'!default;  // Updated automatically by the build script.
-$ext-trial: true!default; // Updated automatically by the build script.
+$ext-trial: false!default; // Updated automatically by the build script.
 $ext-beta: false !default; // Updated automatically by the build script.
 @import 'compass/css3';
 
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-web-components-modern/src/Ext/form/TextArea.js node_modules_patch/@sencha/ext-web-components-modern/src/Ext/form/TextArea.js
--- ../node_modules/@sencha/ext-web-components-modern/src/Ext/form/TextArea.js	2026-04-04 02:40:32.596555066 +0000
+++ node_modules_patch/@sencha/ext-web-components-modern/src/Ext/form/TextArea.js	2023-09-11 15:59:42.000000000 +0000
@@ -133,6 +133,7 @@
     'viewModel',
     'weight',
     'width',
+    'wrap',
     'x',
     'xtype',
     'y',
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-web-components-modern/src/common/util.js node_modules_patch/@sencha/ext-web-components-modern/src/common/util.js
--- ../node_modules/@sencha/ext-web-components-modern/src/common/util.js	2026-04-04 02:40:32.674882878 +0000
+++ node_modules_patch/@sencha/ext-web-components-modern/src/common/util.js	2023-09-11 15:59:42.000000000 +0000
@@ -51,7 +51,7 @@
     `
     window.stop();
   };
-  if (scriptIt() != 0) 
+  if (scriptIt() != 0)
 
   console.warn('[Deprecation] error below is expected');
 
@@ -109,9 +109,14 @@
   }
 }
 
+// NOTE patch
 function doSet(me,prop,val) {
   //console.log('doSet: ' + prop)
 
+  // NOTE patch
+  // VueJS set properties as is, without any coercion, for boolean props we need stringify boolean values
+  if ( typeof val === "boolean" ) val = val.toString();
+
   if (prop == 'plugins') {
     return;
   }
@@ -165,7 +170,13 @@
   }
 }
 
+// NOTE patch
 export function filterProp(propertyValue, property, me) {
+
+  // NOTE patch
+  // vue3 converts "hidden" attribute to string, false value converted to the ""
+  if ( property === "hidden" ) return propertyValue === "";
+
   try {
     if (propertyValue == 'error') {
       return me.attributeObjects[property]
diff --recursive --no-ignore-file-name-case -u ../node_modules/@sencha/ext-web-components-modern/src/common/webcomponentsbase.js node_modules_patch/@sencha/ext-web-components-modern/src/common/webcomponentsbase.js
--- ../node_modules/@sencha/ext-web-components-modern/src/common/webcomponentsbase.js	2026-04-04 02:40:32.682985755 +0000
+++ node_modules_patch/@sencha/ext-web-components-modern/src/common/webcomponentsbase.js	2023-10-11 17:23:03.000000000 +0000
@@ -660,6 +660,17 @@
     }
   }
 
+    // XXX patch: handler for vue dynamic attrinutes
+    setAttribute ( name, value ) {
+        if ( typeof value === "function" || typeof value === "object" ) {
+            this.attributeObjects[name] = value;
+
+            super.setAttribute( name, '[object Object]' );
+        }
+        else {
+            super.setAttribute( name, value );
+        }
+    }
 }
 
 WebComponentsBaseComponent.theDeleted = [];
